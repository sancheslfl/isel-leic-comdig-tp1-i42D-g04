import PY
import os
import TestFilesCD

path= ./TestFilesCD
files= os.listdir(path)
def ex2(files):
    for i in range(0, files.length):
        PY.histograma(files[i])


def main():
    for files in TestFilesCD
    ex2(files)